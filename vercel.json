{
  "installCommand": "pnpm install --no-frozen-lockfile && pnpm rebuild lmdb msgpackr-extract",
  "buildCommand": "mkdir -p lingo/metadata-build && NODE_OPTIONS=--max-old-space-size=4096 pnpm exec next build --webpack"
}
